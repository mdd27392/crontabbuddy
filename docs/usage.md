# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m crontabbuddy --help
```

## Configure

Create a config file (JSON or YAML) and point the tool at it:

```bash
python -m crontabbuddy run --config config.json
```